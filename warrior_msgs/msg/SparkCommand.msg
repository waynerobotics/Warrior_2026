# SparkCommand — position setpoint for one SPARK MAX motor controller
string target       # device_name matching the spark_max_driver 'device_name' parameter
float32 setpoint    # target position in rotations (positive = forward)
